spring:
  application:
    name: company-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        #prefix: application
        namespace: ${namespace:dev}
        shared-configs: public-spring-cloud.yaml,public-datasource.yaml
        refresh-enabled: true