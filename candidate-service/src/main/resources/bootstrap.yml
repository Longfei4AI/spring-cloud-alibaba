spring:
  application:
    name: candidate-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        #prefix: application
        namespace: ${namespace:dev}
        shared-configs: public-spring-cloud.yaml,public-seata.yaml,public-datasource.yaml,public-actuator.yaml #,public-mq.yaml,public-mongodb.yaml,public-redis.yaml
        refresh-enabled: true
